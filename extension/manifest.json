{
  "manifest_version": 3,
  "name": "Agent Tab Grouper",
  "version": "0.3.0",
  "description": "Groups CDP-driven automation tabs into a per-session tab group. Driven over CDP via Runtime.evaluate against this extension's service worker.",
  "background": { "service_worker": "background.js" },
  "action": { "default_title": "Open Agent Monitor" },
  "permissions": ["tabs", "tabGroups", "debugger", "alarms"],
  "host_permissions": ["http://127.0.0.1:9223/*"]
}
